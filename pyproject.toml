[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "voxcpmane2"
version = "0.1.0"
description = "VoxCPM2 TTS runtime + HTTP server for Apple Neural Engine"
requires-python = ">=3.10,<3.13"
license = { text = "MIT" }

dependencies = [
    "coremltools==9.0",
    "numpy>=2",
    "safetensors>=0.4.0",
    "ml-dtypes>=0.5.0",
    "soundfile",
    "soxr>=1.0.0",
    "transformers",
    "fastapi",
    "uvicorn",
    "aiofiles",
    "huggingface_hub",
    "sounddevice",
    "tqdm",
    "ftfy>=6.3.1",
    "inflect",
    "wetext",
    "regex",
]

[project.scripts]
voxcpmane2-server = "voxcpmane2.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/voxcpmane2"]
